/*

Package rep provides enumerations and types to model data structures
of StarCraft II replays (*.SC2Replay) decoded by the s2prot package.

The type that models a replay (and everything in it) is Rep.

For usage example and informat sources, see the package doc of s2prot.

*/
package rep
